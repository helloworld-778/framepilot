"use client";

/**
 * Kept as the stable import path for reduced-motion checks. The implementation
 * now lives with the other media-preference hooks.
 */
export { usePrefersReducedMotion } from "@/lib/use-media-query";
